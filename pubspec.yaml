name: homeautomation
description: IoT group 7 coursework - Home Automation

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_hooks: ^0.18.5+1
  mqtt_client: ^9.7.3
  flutter_inset_box_shadow: ^1.0.8
  provider: ^6.0.5

dev_dependencies:
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
   - assets/icons/
